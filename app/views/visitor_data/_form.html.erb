<%= form_with(model: visitor_datum, local: true) do |form| %>
  <% if visitor_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visitor_datum.errors.count, "error") %> prohibited this visitor_datum from being saved:</h2>

      <ul>
      <% visitor_datum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating %>
  </div>

  <div class="field">
    <%= form.label :review %>
    <%= form.text_field :review %>
  </div>

  <div class="actions">
    <%= form.submit %>
    <%= link_to 'Reviews', 'Reviews' %>
  </div>
<% end %>
